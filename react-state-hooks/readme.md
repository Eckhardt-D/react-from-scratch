# React hooks example

This is an example of the newly (and wonderfully introduced react hooks). It's an alternative way to manage our state.

## Dynamic input example

This directory contains code that uses the following react features.

 - Components
 - State
 - Event Handling
 - Hooks

Event handling has a directory of it's own incase it's not clear what's going on, check that out first. 